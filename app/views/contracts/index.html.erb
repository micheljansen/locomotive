<h1>Listing contracts</h1>

<table>
  <tr>
  </tr>

<% for contract in @contracts %>
  <tr>
    <td><%= link_to 'Show', resource(contract) %></td>
    <td><%= link_to 'Edit', resource(contract, :edit) %></td>
    <td><%= link_to 'Delete', resource(contract, :delete), :confirm => 'Are you sure?' %></td>
  </tr>
<% end %>
</table>

<br />

<%# link_to 'New contract', resource(@client, :contracts, :new) %>
