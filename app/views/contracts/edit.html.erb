<h1>Editing contract</h1>

<%= form_for(@contract, :action => resource(@contract)) do %>
  <%= error_messages %>

  <p>
    <%= submit "Update" %>
  </p>
<% end =%>

<%= link_to 'Show', resource(@contract) %> |
<%= link_to 'Back', resource(:contracts) %>
