%h1== New release for #{@service.name}

- form_for([@service, @release]) do |f|
# TODO = f.error_messages
  %p= f.label :name
  = f.text_field :name
  = f.hidden_field :service_id
  = f.submit "Create"
=link_to "Back to #{@service.name}", @service.id