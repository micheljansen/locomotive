<p>
  <b>Name:</b>
  <%=h @server.name %>
</p>

<p>
  <b>Hostname:</b>
  <%=h @server.hostname %>
</p>

<p>
  <b>Roles:</b>
  
  <table>
  <% @server.fulfillments.each do |fulfillment| %>
    <tr>
      <td><%=h fulfillment.role.name %></td>
      <td><%= link_to 'Show', server_fulfillment_path(@server, fulfillment) %></td>
      <td><%= link_to 'Remove', server_fulfillment_path(@server, fulfillment), :confirm => 'Are you sure? (this does not work yet)', :method => :delete %></td>
    </tr>
  <% end %>
  </table>
  
  <%= link_to 'Add another role' %>
  
</p>


<%= link_to 'Edit', edit_server_path(@server) %> |
<%= link_to 'Back', servers_path %>
