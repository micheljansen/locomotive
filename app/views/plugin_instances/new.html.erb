<h1>New instance of <%=h @plugin.name %></h1>

<p>
  Let's hook a new instance of <%=h @plugin.name %> into your Locomotive. Please provide a name for this instance and configure its properties.
</p>

<%= form_for(@plugin_instance, :action => resource(:plugin_instances)) do %>
  <%= error_messages %>

  <p>
    <%# f.label :plugin_type %>: <%=h @plugin_instance.plugin_type %>
    <%= hidden_field :plugin_type %>
  </p>
  <p>
    <%# f.label :version %>: <%=h @plugin_instance.version %>
    <%= hidden_field :version %>
  </p>
  <p>
    <%= text_field :name, :label => 'Name' %>
  </p>
  <p>
    <%= submit "Create" %>
  </p>
<% end =%>

<%= link_to 'Back', resource(:plugin_instances) %>
