<% puts "server template:" %>
<% p platform %>
<% p servers %>

<h2><%= type == :other ? "Other servers" : h(platform.name) %></h2>
<div id="servers_options">
  <%= link_to "Edit...", edit_platform_path(platform) %>
</div>

<%= render :partial => "/platforms/server", :collection => servers %>