<h1>Administration dashboard</h1>

<p>
  You currently have 
  <%= link_to count_objects(Client), resource(:clients) %>,
  <%= link_to count_objects(Service), resource(:services) %> and
  <%= link_to count_objects(Server), resource(:servers) %> set-up.
  If you want to know more about these, click on any of the tabs above. You can also jump right into adding more by hitting the buttons in the side bar to the right.
</p>

<% throw_content(:sidebar) do %>
  <h2>Actions</h2>
  <ul>
    <li><%= link_to "add a client", resource(:clients, :new) %></li>
    <li><%= link_to "add a service", resource(:services, :new) %></li>
    <li><%= link_to "add a server", resource(:servers, :new) %></li>
  </ul>
<% end %>
